# 카드뉴스 릴스·AI뉴스 생성기(scripts/gen_reel.py, gen_news.py)가 쓰는 파이썬 패키지.
# 설치: pip install -r scripts/requirements-media.txt
# 영상 인코더는 npm 의 ffmpeg-static(node_modules) 을 쓰므로 여기 없음.
Pillow>=10        # 프레임 렌더(카드 합성·자막)
edge-tts>=7       # AI 뉴스 한국어 음성(온라인, 무료) — gen_news.py 전용
