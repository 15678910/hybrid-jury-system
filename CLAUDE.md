# 시민법정 프로젝트 지침

## 🚨 필수 백업 규칙 (절대 위반 금지)

### 데이터 삭제/수정 전 반드시 백업
**어떤 이유로든 Firestore 데이터를 삭제하거나 대량 수정하기 전에 반드시 백업을 실행해야 합니다.**

```bash
# Firestore 백업 실행 (필수!)
cd functions && node backup_firestore.js
```

### 백업 시점
1. **데이터 삭제 전**: posts, videos, signatures 등 어떤 컬렉션이든 삭제 전 필수
2. **대량 데이터 수정 전**: 여러 문서를 한번에 수정하기 전
3. **작업 시작 전**: 새로운 세션에서 데이터 관련 작업 시작 전
4. **배포 전**: 중요한 변경사항 배포 전

### 백업 파일 위치
- `backups/firestore_backup_YYYY-MM-DDTHH-MM-SS.json`
- 최소 7일간 보관 필수

### 위반 시 결과
- 2026-02-08: 백업 없이 블로그 글 전체 삭제 → **복구 불가, 데이터 영구 손실**

---

## 프로젝트 개요
- **이름**: 시민법정 (시민법관 참심제)
- **기술 스택**: React + Vite + Firebase + Tailwind CSS
- **도메인**: https://시민법정.kr (xn--lg3b0kt4n41f.kr), https://siminbupjung-blog.web.app

## 응답 언어
- 한국어로 응답

## 주요 파일 구조
```
src/
├── App.jsx              # 메인 홈페이지
├── main.jsx             # 라우팅 (React.lazy 코드 분할 적용)
├── components/
│   ├── Header.jsx       # 공통 헤더
│   ├── icons/index.jsx  # 공통 SNS 아이콘
│   └── LoginModal.jsx   # 로그인 모달
├── pages/
│   ├── Blog.jsx         # 블로그 목록
│   ├── BlogPost.jsx     # 블로그 상세
│   ├── Videos.jsx       # 동영상 목록
│   └── ...
├── lib/
│   ├── firebase.js      # Firebase 설정
│   └── auth.js          # 인증 관련
functions/
└── index.js             # Firebase Functions (텔레그램 봇, SSR 등)
```

## 수정 시 주의사항

### 수정 금지 영역
- `functions/index.js`의 크롤러 감지 로직 (`isCrawler` 부분) - SNS 미리보기에 영향
- SNS 공유 URL 도메인 (`xn--lg3b0kt4n41f.kr`) - 한글 도메인 인코딩

### Firebase 배포
```bash
npm run build && firebase deploy --only hosting  # 프론트엔드
firebase deploy --only functions                  # 백엔드 함수
```

## 코딩 스타일 규칙

### JavaScript/JSX 필수 규칙
1. **console.log 금지**: 프로덕션 코드에서 `console.log` 사용 금지 (훅으로 경고 표시됨)
2. **불변성 유지**: 객체/배열 수정 시 스프레드 연산자 사용
   ```jsx
   // 나쁜 예
   state.items.push(newItem);

   // 좋은 예
   setItems([...items, newItem]);
   ```
3. **에러 처리**: async/await는 반드시 try-catch로 감싸기
   ```jsx
   try {
     const data = await fetchData();
   } catch (error) {
     console.error('데이터 로드 실패:', error);
     // 사용자에게 에러 표시
   }
   ```

### 보안 규칙
1. **시크릿 하드코딩 금지**: API 키, 토큰 등은 절대 코드에 직접 작성 금지
2. **환경 변수 사용**: 민감 정보는 `.env` 파일 또는 Firebase 환경 설정 사용
   ```jsx
   // 나쁜 예
   const API_KEY = "sk-1234567890";

   // 좋은 예
   const API_KEY = import.meta.env.VITE_API_KEY;
   ```
3. **Firebase 규칙**: Firestore/Storage 보안 규칙으로 데이터 접근 제어

### React 패턴
1. **Custom Hooks**: 재사용 로직은 `use` 접두사로 커스텀 훅 분리
2. **컴포넌트 분리**: 300줄 이상 컴포넌트는 분리 검토
3. **Props 검증**: 중요 props는 기본값 설정 또는 타입 체크

## SNS 공유 설정
- **카카오톡**: SDK 키 `[VITE_KAKAO_KEY 환경변수 참조 — .env.local 또는 deployment env]`, `Kakao.Share.sendDefault`로 공유 (DOM의 `og:image` 메타태그에서 이미지 읽음)
- **X(Twitter)**: 반드시 `https://twitter.com/intent/tweet?text=...&url=...` 사용 (⚠️ `x.com/intent/post` 절대 금지 - HTTP 400 발생)
- **페이스북**: 클립보드 복사 방식 사용 (sharer API 불안정)
- **텔레그램**: 캐시 무효화를 위해 URL에 타임스탬프 추가
- **이미지 전달 흐름**: BlogPost.jsx → SEOHead(og:image 메타태그) → SNSShareBar.getOgImage()(DOM에서 읽기) → 카카오/SNS에 전달

## 성능 최적화 (적용됨)
- React.lazy() 코드 분할
- Promise.all() 병렬 데이터 페칭
- 로컬 스토리지 캐싱 (블로그, 동영상)
- 공통 아이콘 컴포넌트

## 관리자 기능
- `/admin` - 서명 관리
- `/blog/admin` - 블로그 관리
- `/videos/admin` - 동영상 관리
- `/governance/admin` - 거버넌스 관리

## 자주 발생하는 실수 (반복 방지)

### 🚨 2026-07-07 사건 (블로그 글 삭제→재작성 = 공유 링크 영구 파손)
- **사건**: `/blog/VjKO7SPGvfHVDXs0gInQ` 등 이미 SNS·검색에 공유된 블로그 링크가 "글을 찾을 수 없습니다"(404)로 깨짐
- **원인**: 글을 **삭제 후 새로 작성**하면 Firestore 자동 문서 ID가 바뀜 → 옛 ID로 된 모든 공유 링크(트위터·카카오·검색)가 404. 삭제된 옛 ID는 방문통계(analytics)에만 흔적이 남고 `posts` 컬렉션엔 존재하지 않음
- **절대 규칙**:
  1. **글 내용 변경은 반드시 `/blog/admin`의 [수정] 버튼 사용** — 글 ID가 유지돼 링크가 안 깨짐. **삭제 후 재작성 절대 금지**
  2. **부득이 삭제·재작성한 경우**: `src/data/postRedirects.js`에 `'옛ID': '새ID'` 한 줄 추가 → 옛 링크가 새 글로 자동 리다이렉트되어 복구됨
  3. **AI·자동화 스크립트도 동일**: 블로그 글을 프로그램으로 지웠다가 다시 쓰는 로직 금지 → 반드시 `updateDoc`(수정)으로 처리. 같은 날짜 사법뉴스 중복 생성 금지(생성 전 동일 date 존재 여부 확인)
  4. **삭제 전 백업 필수**(최상단 백업 규칙과 동일): `posts` 삭제 전 `cd functions && node backup_firestore.js`
- **관련 코드**: `BlogPost.jsx`(POST_REDIRECTS 자동 이동), `AdminBlog.jsx`(삭제 경고 confirm 이미 존재), `postRedirects.js`(리다이렉트 맵)

### 🚨 2026-03-18 사건 (정당 입장 데이터 오염 - 절대 반복 금지)
- **사건**: 민주당 검찰개혁안 업데이트 시, 사용자의 제안(주권자사법개혁추진준비위원회 안/핀란드식 모델)의 내용을 민주당 공식 입장으로 잘못 기재
- **오염된 데이터**: 시민기소심사위원회(강제기소권), 참심제(시민3+법관1), 법률감찰관+사법옴부즈만 → 이것은 전부 **주권자사법개혁추진준비위원회 안**이지 민주당 안이 아님
- **원인**: 사용자가 제공한 이미지의 두 번째 표(헌법 제1조 제2항 구현)를 민주당 입장으로 착각. 실제로는 사용자의 제안
- **교훈**:
  1. **출처 확인 필수**: 정당/기관 입장에는 해당 정당이 실제 발표한 내용만 기재
  2. **주권자사법개혁추진준비위원회 안 vs 타 정당 안 구분**: 사용자 제안(핀란드 모델)을 다른 정당 입장으로 절대 혼동 금지
  3. **원칙 vs 구체안 구분**: "국민 통제 필요" 원칙만 언급한 경우, 구체적 제도(시민기소심사위 등)를 해당 정당이 주장한 것으로 기재 금지
  4. **확인 불가 시 질문**: 출처가 불분명한 내용은 사용자에게 확인 후 기재
  5. **비교 시 주체 명확 표시**: "민주당 안 일치"가 아닌 "주권자사법개혁추진준비위원회 안" vs "민주당 안" 명확 구분

### 🚨 2026-03-18 사건 추가 (법률 정보 부정확 - 절대 반복 금지)
- **사건**: 검찰개혁 수정안 정보 정리 시 다수 오류 발생
- **오류 목록**:
  1. "영장청구권 삭제"로 표기 → 실제로는 "영장 집행 지휘권"만 삭제, 헌법 제12조 제3항에 의해 영장청구권 자체는 공소청 검사에게 존속
  2. "검찰총장 직무위임·이전·승계권 삭제"로 표기 → 실제로는 "법무부장관의" 해당 권한 삭제
  3. "보완수사권 폐지"를 현재 수정사항으로 표기 → 실제로는 지방선거 후 형사소송법 개정에서 별도 논의 예정
- **근본 원인**:
  1. **추론으로 정보 생성**: 뉴스 원문을 충분히 검색·확인하지 않고 추측으로 채움 (hallucination)
  2. **법적 구조 미파악**: 헌법→법률→시행령 위계를 이해하지 못해 "삭제"의 법적 의미 오해
  3. **1차 자료 미검증**: PDF 법률안을 읽었으나 수정안 변경사항은 별도 검증 없이 기재
- **재발 방지 원칙**:
  1. **법률/정책 정보는 반드시 뉴스 원문 또는 1차 자료에서 직접 인용** — 추론·추측 절대 금지
  2. **"삭제"의 법적 범위 확인**: 특정 법에서 삭제 vs 상위법(헌법·형사소송법)에서 존속 구분 필수
  3. **현재 확정 vs 향후 논의 구분**: 시행 시점, 논의 예정 사항을 명확히 분리
  4. **모르면 "모른다" 표시**: 확인 불가한 정보는 빈칸으로 두고 사용자에게 질문
  5. **퍼플렉시티 수준의 검증**: 정보 기재 전 WebSearch로 최소 2개 이상 뉴스 출처 교차 확인

### 🚨 2026-08-31 사건 (검색 요약이 서로 다른 사건을 뒤섞음)
- **사건**: 재판 일정을 갱신하면서 WebSearch 요약만 보고 판단했더니, 요약이 **다른 사건의
  날짜를 끌어다 붙이고** 있었다 — 위증 사건(서울고법 형사1부·윤성식)의 날짜를
  내란우두머리 항소심(형사12-1부·이승철) 것으로, **대법원 상고심 선고를 항소심 선고로** 제시했다.
- **원인**: 같은 피고인(윤석열)의 재판이 8건이라 이름으로 검색하면 사건이 섞인다.
  검색 요약은 그 섞임을 구분하지 못한 채 하나의 사건인 것처럼 문장을 만든다.
- **재발 방지**:
  1. **검색 요약을 그대로 옮기지 말 것.** 요약은 「어느 기사를 열어 볼지」 고르는 데만 쓰고,
     기재할 사실은 **기사 원문을 하나씩 열어** 확인한다.
  2. **사건번호로 특정한다.** 이름으로 찾으면 섞이지만 사건번호는 하나만 가리킨다
     (예: 윤석열 내란우두머리 항소심 = **2026노648**). 기사에 사건번호가 없으면
     재판부·심급·날짜 세 가지가 모두 일치하는지 대조한다.
  3. **1차 자료 우선** — 대법원 「나의 사건검색」에 사건번호를 넣으면 기일이 공개된다.
     언론 요약보다 정확하고 기일 변경도 반영돼 있다.
  4. **심급을 반드시 구분해 적는다.** 1심·항소심·상고심의 선고를 섞으면 「무기징역이
     확정됐다」 같은 치명적 오보가 된다.
  5. 확인이 안 되면 **비워 두고 「아직 확인하지 못했다」를 데이터에 명시**한다.
     추정으로 채우지 않는다.
- **원격 세션의 한계**: 클라우드 세션은 언론 도메인이 차단돼 기사 원문을 열 수 없다.
  즉 **원격에서는 이 검증을 할 수 없다.** 재판 일정·사건 사실은 데스크탑 세션에서 잡는다.

### 🚨 2026-03-20 사건 (App.jsx 네비게이션 누락 - 반복 발생)
- **사건**: 새 페이지(모의재판 체험) 추가 시 Header.jsx만 수정하고 App.jsx의 네비게이션을 수정하지 않음
- **결과**: 서브페이지에서는 소통방 메뉴에 "모의재판 체험"이 보이지만, 메인 페이지(시민법정.kr)에서는 "의사결정"만 표시
- **원인**: 메인 홈페이지(App.jsx)와 서브페이지(Header.jsx)에 **별도의 네비게이션이 존재**하는 구조를 인지하지 못함
- **교훈**:
  1. **네비게이션 수정 시 반드시 2곳 모두 확인**: `App.jsx`(메인 페이지) + `Header.jsx`(서브페이지)
  2. 배포 후 **메인 페이지(시민법정.kr)**에서 메뉴 확인 필수 (서브페이지만 확인하면 안 됨)
  3. 빌드 번들에서 해당 텍스트가 포함되었는지 `grep`으로 검증

### 🚨 2026-02-08 사건 (절대 반복 금지)
- **사건**: 백업 없이 Firestore의 블로그 글 전체 삭제
- **결과**: 원본 블로그 글 7개 + 이전 사법뉴스 전체 영구 손실
- **원인**: 데이터 삭제 전 백업 미실행
- **교훈**: 어떤 데이터든 삭제/수정 전 반드시 `node backup_firestore.js` 실행

### 절대 수정 금지
- AI 모델 기본값: `useState('claude')` — 임의 변경 금지 (2026-02-24 사건)
- `functions/index.js`의 `isCrawler` 로직 → SNS 미리보기(카카오톡, 페이스북, 텔레그램) 깨짐
- SNS 공유 URL의 도메인 `xn--lg3b0kt4n41f.kr` → 한글 도메인 인코딩이므로 변경 시 공유 링크 전체 실패
- 카카오톡 SDK 키 변경 → SNS 공유 기능 중단
- 사용자 명령 없이 임의로 설정/로직 변경 절대 금지

### 이미지/사진 관련
- `public/` 폴더의 인물 사진(PNG)은 auto-crop 처리 완료 상태 → 재업로드 시 동일하게 crop 필요
- 위키미디어 이미지 URL은 `/thumb/.../{크기}px-` 형식 유지 필요
- 인물 사진 컨테이너: `w-12 h-12 rounded-full object-cover` → 크기 변경 시 전체 레이아웃 확인

### 배포 관련
- 프론트엔드만 수정 시 `firebase deploy --only hosting` 사용 (functions 배포 불필요)
- `npm run build` 실패 시 배포 절대 금지
- `.firebase/hosting.ZGlzdA.cache`는 자동 생성 파일 → 커밋에 포함해도 무관하나 수동 수정 금지

### Git 커밋 검증 규칙 (2026-03-06 추가)
- **커밋 전 필수 확인**: `git status`로 수정된 파일 목록과 staging된 파일 목록 비교
- **`git add` 시 특정 파일 지정**: `git add -A` 대신 변경 파일을 명시적으로 나열
- **핵심 데이터 파일 우선 확인**: `judges.js`, `firebase.json`, `main.jsx` 등 핵심 파일이 staging에 포함되었는지 반드시 확인
- **커밋 메시지와 실제 변경 내용 일치**: 커밋 메시지에 "판사 추가", "데이터 수정" 등이 포함되면 해당 데이터 파일이 반드시 커밋에 포함되어야 함

### 🚨 2026-03-06 사건 (App Check Enforce 장애)
- **사건**: App Check를 Firestore/Storage에 Enforce 적용 → 사이트 전체 데이터 0으로 표시
- **원인**: reCAPTCHA 토큰이 100% Unverified 상태에서 Enforce 적용 → 모든 요청 차단
- **교훈**: Firebase Console에서 보안 설정 변경 시 반드시 다음 순서를 따를 것:
  1. Monitoring 모드로 먼저 적용 → 토큰 Verified 비율 확인
  2. Verified 비율이 90% 이상일 때만 Enforce 전환
  3. Enforce 후 즉시 사이트 접속하여 데이터 로딩 확인
  4. 문제 발생 시 즉시 Unenforce로 롤백
- **현재 상태**: Cloud Firestore = Monitoring, Storage = Unenforced

### 🚨 2026-03-06 사건 (백대현 판사 데이터 유실)
- **사건**: `judges.js`에 백대현 판사 추가 후 git commit 시 해당 파일을 staging하지 않아 데이터 유실
- **원인**: `git add`에서 수정된 `judges.js`를 누락, 커밋 메시지만 "백대현 판사 추가"로 작성
- **2차 오류**: 복구 시 `cases` 필드를 `{title, date, description}` 형식으로 작성 → React Error #31 발생
- **교훈**:
  1. `git add` 후 반드시 `git status`로 모든 수정 파일이 staging되었는지 확인
  2. 커밋 전 `git diff --cached`로 실제 커밋 내용 검증
  3. judges.js의 cases 필드는 반드시 `{ text: '...', source: '...' }` 형식 사용
  4. 판사 추가 후 라이브 사이트에서 해당 카테고리 탭 클릭하여 실제 표시 확인

### 🚨 2026-03-08 사건 (김용현 AI 점수 인코딩 장애)
- **사건**: curl에서 한국어 파라미터(`defendant=김용현`)를 보내 AI 평가 실행 → Firestore에 깨진 문서 키(U+FFFD)로 저장됨
- **원인**: Windows curl이 한국어를 CP949/깨진 UTF-8로 전송 → Cloud Function이 U+FFFD로 받음 → 잘못된 키로 문서 생성
- **2차 원인**: AI가 프롬프트의 "100점 금지" 지시를 무시하고 검찰 공정성 100점 반환
- **교훈**:
  1. curl로 한국어 파라미터 전송 금지 — 브라우저 또는 Postman 사용
  2. AI 점수는 프롬프트만으로 제어 불가 — 코드 레벨 클램핑 필수
  3. Firestore 문서 키에 한국어 사용 시 인코딩 검증 필수
- **적용된 방어 조치**:
  - `safeDecodeKorean()` 유틸리티 함수 (U+FFFD 감지 + 거부)
  - Step B 후 점수 클램핑: 빈 이슈 배열 → 최대 75점, 절대 최대 99점

### AI 점수 출력 후처리 규칙 (2026-03-08 추가)
- `evaluateJudicialIntegrity` 함수의 Step B 결과에 반드시 코드 레벨 클램핑 적용
- 100점 절대 금지: `Math.min(score, 99)` 적용
- 빈 이슈 배열 → 해당 점수 최대 75점: `Math.min(score, 75)`
- 클램핑 로직 위치: Step B JSON 파싱 직후, 결과 병합 직전
- **이 클램핑 로직을 제거하거나 우회하지 말 것**

### 한국어 파라미터 안전 처리 규칙 (2026-03-08 추가)
- `functions/index.js`에서 한국어 파라미터 수신 시 반드시 `safeDecodeKorean()` 사용
- 중복 try-catch 패턴 대신 유틸리티 함수 호출
- U+FFFD 감지 시 null 반환 → 400 에러로 응답

### CI 테스트 규칙 (2026-03-08 추가)
- `.github/workflows/ci.yml`의 테스트 단계를 절대 주석 처리하지 말 것
- `judges.js` 스키마 검증 테스트(`src/data/judges.test.js`)는 삭제 금지
- 새 데이터 파일 추가 시 해당 스키마 검증 테스트도 함께 추가

### 보안/인프라 변경 시 자체 검증 필수
- Firebase 보안 규칙, App Check, CORS 등 변경 후 **반드시 사이트 접속 테스트**
- 배포 후 최소 3개 페이지 확인: 메인, 서명, 블로그
- 데이터가 0이거나 빈 화면이면 즉시 롤백 후 원인 분석

### 🚨 2026-03-14 사건 (Firestore 필드명 불일치 → 카카오톡 잘못된 이미지)
- **사건**: 블로그 글 카카오톡 공유 시 해당 글의 이미지가 아닌 기본 사이트 이미지(`og-image.jpg`)가 전달됨
- **원인**: `AdminBlog.jsx`는 Firestore에 `imageUrl` 필드로 저장하는데, `BlogPost.jsx`는 `post.featuredImage || post.thumbnailUrl`로 읽음 → 항상 `undefined` → SEOHead가 기본 OG 이미지 렌더링 → 카카오 SDK가 기본 이미지 전달
- **교훈**:
  1. **Firestore 필드명 일관성 검증 필수**: 데이터 저장(write) 코드와 읽기(read) 코드의 필드명이 반드시 일치하는지 확인
  2. 새 기능 추가 시 AdminBlog의 저장 필드명(`imageUrl`, `title`, `content`, `summary`, `author`, `date`, `category`)을 기준으로 읽기
  3. SNS 공유 수정 시 반드시 실제 공유 테스트 (카카오톡, X 등)
- **Firestore posts 컬렉션 필드명 (정본)**:
  ```
  title, content, summary, imageUrl, author, date, category,
  writerCode, views, likes, likedIPs, createdAt
  ```

### 🚨 2026-03-14 사건 (X/Twitter 공유 API 엔드포인트 오류)
- **사건**: X 공유 버튼 클릭 시 HTTP 400 에러
- **원인**: `x.com/intent/post`는 유효하지 않은 엔드포인트. 이전에 `intent/tweet` → `intent/post`로 잘못 변경했음
- **교훈**:
  1. **X(Twitter) 공유 URL은 반드시 `https://twitter.com/intent/tweet`** 사용 (이것이 공식 API)
  2. `x.com/intent/post`는 존재하지 않는 엔드포인트 → HTTP 400
  3. SNS API 변경 시 반드시 실제 브라우저에서 공유 테스트 후 배포

### 🚨 2026-03-14 사건 (이미지 잘림 - CSS aspect-ratio + object-cover)
- **사건**: 블로그 카드 및 본문의 이미지 상단 제목 텍스트가 잘려서 표시
- **원인**: `aspect-video`(16:9) + `object-cover`가 이미지를 강제 비율로 잘라냄. `max-h-96`이 높이를 384px로 제한
- **교훈**:
  1. 이미지에 텍스트가 포함된 카드뉴스/OG이미지는 `object-cover` 사용 금지
  2. Blog.jsx 이미지: `aspect-video`와 `object-cover` 없이 원본 비율 유지
  3. BlogPost.jsx 이미지: `max-h-96`과 `object-cover` 없이 원본 비율 유지
  4. 이미지 잘림 수정 시 목록 페이지(Blog.jsx)와 상세 페이지(BlogPost.jsx) 모두 확인

### 카카오 로그인
- 이메일 권한은 비즈앱 전환 필요 → 현재 `profile_nickname`, `profile_image`만 사용
- 모바일/데스크톱 로그인 플로우가 다름 (리다이렉트 vs 팝업) → 한쪽만 수정하지 말 것

## 작업 템플릿

### 코드 수정 요청 시
1. 수정 대상 파일을 먼저 읽고 현재 상태 파악
2. `functions/index.js`의 `isCrawler` 로직 영향 여부 확인
3. SNS 공유 URL이 포함된 경우 도메인 인코딩 검증
4. 수정 완료 후 `npm run build`로 빌드 확인

### 새 페이지 추가 시
1. `src/pages/`에 컴포넌트 생성
2. `src/main.jsx`에 `React.lazy()` 라우트 추가
3. **⚠️ 네비게이션 메뉴 2곳 모두 수정 필수**:
   - `src/App.jsx` — **메인 홈페이지 전용 네비게이션** (소통방 드롭다운 + 모바일 메뉴)
   - `src/components/Header.jsx` — **서브페이지 공통 헤더** (소통방 드롭다운 + 모바일 메뉴)
   - **두 파일의 메뉴 항목이 반드시 동기화되어야 함** (한쪽만 수정하면 메인/서브 페이지 메뉴가 달라짐)
4. `functions/index.js`에 SSR 메타태그 추가 (SEO 필요 시)
5. **SNS 공유용 OG 이미지 설정 필수**: 각 페이지별 고유 OG 이미지를 설정해야 카카오톡/페이스북/X 공유 시 해당 페이지의 이미지가 표시됨
   - `functions/index.js`의 `createStaticPageHandler`에 이미지 URL 전달
   - 또는 페이지 컴포넌트에서 `SEOHead`의 `image` prop으로 페이지별 이미지 지정
   - OG 이미지 없으면 기본 사이트 이미지(`og-image.jpg`)가 표시되어 어떤 페이지를 공유했는지 구분 불가

### 인물 사진 추가/교체 시
1. 위키미디어 검색 우선 → 없으면 공식 사진(korea.kr) → 없으면 사용자 업로드
2. 사용자 업로드 PNG는 Python Pillow로 auto-crop (투명/흰색 여백 제거)
3. `SentencingAnalysis.jsx`의 `PERSON_PHOTOS` 객체에 경로 추가
4. 빌드 후 배포

## 🖥️ 작업 환경 — 원격인가 데스크탑인가 (2026-08-08 추가)

**이 프로젝트는 두 환경에서 돌아간다. 무엇을 할 수 있는지가 환경마다 다르므로,
세션을 시작하면 AI 는 먼저 자신이 어디에 있는지 밝히고 그에 맞게 안내해야 한다.**

### 기본은 데스크탑이다

원격 세션은 **데스크탑 폴더를 볼 방법이 아예 없다.** 설정으로 여는 문제가 아니라
다른 컴퓨터라서 구조적으로 불가능하다. 그래서 `public/` 에 넣은 이미지 하나를 쓰는
데도 push 를 거쳐야 하고, 함수 배포는 아예 막히며, 화면 확인도 못 한다.

- **평소에는 데스크탑 앱으로 켠다.** 폴더를 직접 보므로 파일 왕복이 없고,
  함수 배포·로컬 프리뷰·판례 수집이 모두 된다.
- **외출 중에도 데스크탑을 쓴다.** 크롬 원격 데스크톱(`remotedesktop.google.com`)으로
  데스크탑 화면에 들어가 **거기서 데스크탑 앱을 켜면** 제약이 전혀 없다. 데스크탑을
  켜 둔 채(절전 해제) 나가는 것이 조건이다. 이 경우 `public/` 에 넣은 파일은 커밋하지
  않아도 바로 보인다 — 그 화면이 데스크탑 자체이기 때문이다. **단, 그 화면에서
  브라우저를 열어 대화를 이어 열면 그 순간 클라우드가 된다**(아래 함정 참조).
- **원격 세션은 데스크탑이 꺼져 있을 때의 대안이다.** 오래 걸리는 수집을 걸어 두고
  나갈 때 정도가 쓸모다. 그 세션에서는 함수 배포와 화면 확인이 불가능하다는 것을
  전제로 계획을 짠다.

커밋은 파일을 AI 에게 보이게 하려고 하는 것이 아니라 ⑴ GitHub Actions 배포에 나가려면
git 에 있어야 하고 ⑵ 기기가 고장 나도 남기기 위해 한다. 데스크탑 작업 중에는 급하지
않으니 작업을 마치고 함께 커밋하면 된다.

### ⚠️ 가장 헷갈리는 함정 — 「어느 앱이냐」가 아니라 「어떤 세션이냐」가 가른다

**앱과 세션은 별개다.** 데스크탑 앱은 사용자의 컴퓨터에서 돌지만, 그 안에서 여는 **대화**는
로컬일 수도 클라우드일 수도 있다. 앱이 리모컨 노릇을 하고 작업은 저쪽 컴퓨터에서
벌어지는 형태가 가능하다는 뜻이다. 그러므로 **앱의 종류로는 판별할 수 없다.**

| 세션의 종류 | 실제로 도는 곳 | 폴더가 보이나 |
|---|---|---|
| **로컬 폴더를 열어 시작한 세션** (앱·터미널 어느 쪽이든) | 그 컴퓨터 | ✅ 실제 폴더 |
| **클라우드 세션** (사이드바에서 이어 열거나 웹에서 시작) | **클라우드 VM** | ❌ GitHub 복제본만 |

**⭕ 판별은 이것 하나뿐이다 — AI 가 직접 확인한다**

```bash
ls functions/.env     # 있으면 로컬, 없으면 클라우드
```

`functions/.env` 는 `.gitignore` 로 막혀 **GitHub 을 건너올 수 없다.** 그러므로 이 파일이
보인다는 것은 실제 폴더를 직접 보고 있다는 뜻이다. (`.env.local`, `.claude/launch.json` 도
같은 성질이라 교차 확인에 쓸 수 있다.)

**❌ 화면을 보고 판별하려는 시도는 전부 실패했다** (2026-08-17 에 세 번 잘못 안내했다):
- 「주소창이 보이면 브라우저(클라우드), 안 보이면 앱(로컬)」 — **틀렸다.**
  데스크탑 앱 안에서도 클라우드 세션이 열린다.
- 「데스크탑 앱이면 로컬이다」 — **틀렸다.** 같은 이유다.
- 「입력창 위의 브랜치 칩과 「PR 생성」 버튼이 보이면 클라우드다」 — **틀렸다.**
  로컬 세션에도 똑같이 표시된다(사용자 스크린샷으로 확인). 변경량 숫자도 마찬가지다.

**화면 표시로 판별하려 들지 마라.** 창의 모양·버튼·아이콘은 로컬과 클라우드가 같을 수 있다.
`ls functions/.env` 한 줄이면 끝나는 일을 눈짐작으로 대신하다가 하루를 더 썼다.

참고로 제목 앞의 **구름 아이콘 ☁** 은 클라우드 세션에 붙는 것으로 보이나, 위의 세 번을
겪은 뒤이므로 **이것도 보조 참고로만 쓰고 판정은 `ls functions/.env` 로 한다.**

클라우드 세션으로 가게 되는 길은 여럿이라 자기도 모르게 걸린다: 사이드바에서 예전 대화
클릭, 브라우저 `claude.ai/code`, 북마크, **이메일·알림의 링크 클릭**, 검색 결과 클릭.
공통점은 **「이어서 열었다」**는 것이다.

사이드바의 세션 목록은 클라우드에 저장된 세션들이다. 로컬 폴더로 켠 세션은 거기에
나타나지 않는다. 그러므로 **예전 세션을 이어 여는 것은 로컬 세션이 아니다.** 로컬 세션에는
「이어서 열기」가 없고 매번 새로 시작한다 — 이전 맥락은 `docs/SESSION_HANDOFF_*.md` 로 넘긴다.

2026-08-08 에 이것 때문에 하루를 소모했다 — 사용자는 데스크탑 앞에 있었고 `public/` 에
이미지를 넣었는데, AI 는 클라우드에서 돌고 있어 그 파일을 볼 수 없었다.
2026-08-17 에 같은 일이 반복됐다. 원인은 8월 2일에 시작한 클라우드 세션을 보름 동안
사이드바에서 이어 열고 있었던 것이다. **세션이 도는 곳은 만들어질 때 정해지고 바뀌지 않는다.**
클라우드 세션을 로컬 세션으로 전환하는 방법은 없다 — 새로 시작하는 수밖에 없다.

**로컬 세션으로 여는 법**: 데스크탑 앱에서 **새 대화를 시작하면서 프로젝트 폴더를 지정**한다
(사이드바에서 예전 대화를 클릭하면 안 된다). 터미널로 열려면 탐색기에서 프로젝트 폴더 →
주소창에 `powershell` → 엔터 → `claude`. 앱이 트레이(시계 옆 `∧`)로 내려가 보이지 않으면
시작 메뉴에서 `Claude` 검색으로 띄운다.

**AI 는 사용자의 화면을 볼 수 없다.** 메뉴 이름이나 버튼 위치를 단정해 안내하지 말고,
확실한 것(`ls functions/.env`, 브랜치 칩 유무)만 말하고 나머지는 사용자에게 확인을 구한다.
2026-08-17 에 없는 절차(「시작 메뉴에서 검색」)를 이미 앱을 켜 둔 사용자에게 안내해
혼란을 키웠다.

### 세션 첫 동작 — AI 가 스스로 확인하고 먼저 밝힌다

사용자가 물어보기 전에 확인해서 「데스크탑입니다 / 원격입니다」를 먼저 말한다.
판별은 한 줄이면 된다.

```bash
ls functions/.env    # 있으면 로컬, 없으면 클라우드
```

`functions/.env` 는 `.gitignore` 로 막혀 있어 **GitHub 을 건너올 수 없다.** 그러므로
이 파일이 보인다는 것은 사용자의 실제 폴더를 직접 보고 있다는 뜻이다. 반대로 안 보이면
GitHub 복제본만 보고 있는 것이다. (`.env.local`, `.claude/launch.json` 도 같은 성질이라
교차 확인에 쓸 수 있다.)

**⚠️ 이 판별은 「로컬인가」만 알려 준다 — 「어느 기기인가」는 알려 주지 않는다.**
기기를 알아야 하면 따로 확인한다. **사용자의 말만으로 단정하지 말 것.**

```powershell
hostname                                # 컴퓨터 이름
@(Get-CimInstance Win32_Battery).Count  # 0 이면 데스크탑, 1 이상이면 노트북
```

**원격 화면으로 보는 것은 기기 변경이 아니다.** 파섹·크롬 원격 데스크톱으로 노트북에서
데스크탑에 붙으면 노트북은 화면과 키보드일 뿐이고, 폴더도 Claude Code 도 전부 데스크탑에서
돈다. 그러므로 「노트북에서 작업한다」는 말을 듣고 파일이 안 넘어간다고 넘겨짚지 말 것 —
2026-08-17 에 그렇게 넘겨짚어 있지도 않은 이사를 준비했다. `hostname` 한 줄이면 끝날 일이었다.

원격으로 확인되면 그 자리에서 함께 알린다 — 「이 세션에서는 함수 배포가 안 되고,
`public/` 에 넣으신 파일은 push 하거나 대화창에 첨부해야 제게 도달합니다」.

| | 원격 (claude.ai/code · 웹·앱) | 데스크탑 (로컬 Claude Code) |
|---|---|---|
| 보는 파일 | **GitHub 저장소를 새로 복제한 것** | 실제 로컬 폴더 |
| `public/` 에 파일 넣기 | ❌ 보이지 않음 → push 하거나 대화창 첨부 | ✅ 넣으면 바로 인식 |
| 호스팅 배포 | ✅ AI 가 Actions 로 직접 | ✅ `firebase deploy --only hosting` |
| **함수 배포** | ❌ **불가** (`functions/.env` 없음) | ✅ 가능 |
| law.go.kr · data.go.kr | ⚠️ Actions 경유만, 간헐적 차단 | ✅ 한국 IP 라 바로 됨 |
| 사이트 화면 확인 | ❌ AI 가 접속 못 함 → 사용자가 캡처 | ✅ 로컬 프리뷰로 배포 전 확인 |
| 자리를 비워도 진행 | ✅ | ❌ |

### 환경별로 지켜야 할 것

**원격일 때**
1. **파일 전달은 두 길뿐이다** — `git push`, 또는 대화창에 첨부.
   데스크탑 폴더에 넣는 것만으로는 AI 에게 도달하지 않는다. 배포도 GitHub 의
   파일로 이뤄지므로 push 하지 않으면 사이트에도 반영되지 않는다.
2. **함수 배포가 필요한 변경은 미완으로 남는다.** `functions/index.js` 를 고쳤다면
   호스팅 배포만으로 끝나지 않는다. SSR 메타(크롤러가 읽는 제목·OG 이미지)는
   데스크탑에서 `firebase deploy --only functions:<함수명>` 을 해야 반영된다.
   AI 는 이 사실을 반드시 함께 알려야 한다.
3. **배포 결과를 AI 가 눈으로 확인할 수 없다.** 「확인했다」고 말하지 말고
   사용자에게 확인을 부탁할 것.
4. **`.gitignore` 된 파일은 세션이 끝나면 사라진다.** `docs/private/` 같은 곳에
   남긴 내용은 사용자에게 파일로 전달해 둘 것.

**데스크탑일 때**
1. `public/` 에 넣은 파일은 바로 쓸 수 있다. push 를 기다릴 필요 없다.
2. 함수 배포가 가능한 유일한 환경이다. 밀린 함수 변경이 있으면 여기서 처리한다.
3. 판례·통계 수집(law.go.kr)이 안정적으로 된다. 원격에서 막혔던 수집은 여기서 돌린다.
4. 배포 전 로컬 프리뷰로 화면을 확인할 수 있다(`.claude/launch.json`).

### 맥락을 폴더로 가져오는 법 (2026-08-17 추가)

클라우드 세션과 폴더 세션은 **서로 가진 것이 정반대**다. 둘 다 갖는 방법은 없다.

| | 사이드바 등으로 **이어 연** 세션 | 폴더를 찾아 **새로 연** 세션 |
|---|---|---|
| 실제 폴더 | ❌ GitHub 복제본 | ✅ |
| **지난 대화** | ✅ 이어짐 | ❌ **백지** |

**평소에는 폴더에서 작업한다.** 그러면 pull 이 필요 없다 — 파일이 이미 거기 있다.
`git pull` 이 필요한 경우는 셋뿐이다: ⑴ 클라우드 세션을 썼을 때 ⑵ 진짜 다른 기기일 때
⑶ Actions 가 커밋했을 때. 그 외엔 `git status -sb` 로 확인만 하고 그냥 작업한다.

> ⚠️ **`git pull` 은 맥락을 가져오지 않는다.** 누군가 **파일로 써서 push 한 것**만 온다.
> 아래 ①을 건너뛰고 pull 만 하면 `Already up to date` 가 뜨고 아무 일도 없다.

**클라우드 세션을 썼다면 — 맥락은 git 으로 운반한다.**

**① 클라우드 세션에 그대로 붙여넣는다**

```
지금까지의 대화 맥락을 docs/SESSION_HANDOFF_YYYY-MM-DD.md 로 정리해서 커밋하고 push해줘.

다음 세션은 이 파일만 읽고 이어서 작업해야 하니 아래를 반드시 포함할 것:
- 무엇을 왜 그렇게 결정했는가 (한 일보다 이유가 중요하다)
- 중간에 틀렸다가 바로잡은 것과 그 근거
- 확인하려다 실패한 것과 실패 사유
- 다음에 할 일을 우선순위 순으로
- 현재 브랜치·배포 상태
```

**② 폴더에서 새 세션을 연 뒤**

```bash
git status -sb                                   # 뒤처져 있는지 먼저 확인
git pull origin <브랜치>
```

이어서 첫 마디로 `docs/SESSION_HANDOFF_YYYY-MM-DD.md 읽고 이어서 작업해줘`.

> **클라우드 세션을 이미 닫았어도 늦지 않다.** 사이드바에서 그 대화를 다시 열면 맥락이
> 살아 있으니 거기서 ①만 시키고 닫으면 된다. **클라우드를 폴더 세션으로 바꾸는 것은
> 불가능하지만, 맥락을 꺼내오는 것은 언제든 된다.**

**함정 셋**

| 함정 | 결과 |
|---|---|
| `.gitignore` 된 경로(`docs/private/` 등)에 쓰게 함 | push 돼도 폴더에 안 온다. 추적되는 경로에 쓸 것 |
| 클라우드가 다른 브랜치에 push | `git branch -r` 로 확인 후 pull |
| **「한 일」만 적힌 문서** | 가장 흔한 실패. 커밋 로그를 다시 쓴 것에 불과하다 |

세 번째가 진짜 문제다. 파일과 커밋 이력은 폴더에 이미 있으니 다시 적을 필요가 없다.
**사라지는 것은 「왜」다.**

### 진짜 대책은 끝에 몰아 쓰지 않는 것

인수인계 문서는 마지막 안전망이고, 본 대책은 **결정이 나온 그 자리에서 파일에 적는 것**이다.

| 무엇 | 어디에 |
|---|---|
| 사실을 바로잡은 근거 | 그 자료의 README (예: `docs/bills/README.md`) |
| 확인 실패와 그 사유 | 해당 데이터 파일의 주석·필드 (지우지 말 것) |
| 반복 방지 규칙 | 이 문서의 「자주 발생하는 실수」 |

이렇게 해 두면 세션이 어디서 끊겨도 손실이 거의 없고, 인수인계 문서는
**「아직 파일에 못 적은 것」**만 담으면 된다.

### 어느 쪽이든 공통

- 지정된 브랜치에서만 작업하고, 승인 없이 배포하지 않는다.
- SNS 공유 이미지·제목은 **경로가 둘**이다. 공유 버튼은 화면 DOM 의 `og:image`(호스팅
  배포로 반영), 링크 붙여넣기 미리보기는 크롤러가 읽는 SSR 메타(**함수 배포로 반영**).
  둘 중 하나만 바꾸면 절반만 바뀐다.

---

## 배포 워크플로우 (2026-02-25 업데이트)
**반드시 로컬 프리뷰 확인 후 사용자 승인을 받아 배포한다.**

```
코드 수정 → npm run build → 로컬 프리뷰(preview_start) → 스크린샷 공유 → 사용자 승인 → 배포
```

- 로컬 프리뷰 서버: `.claude/launch.json` → `npm run dev` (port 5173)
- **사용자 승인 없이 `firebase deploy` 절대 금지**
- 프리뷰 확인 도구: `preview_screenshot`, `preview_snapshot`, `preview_inspect` 활용

## 판사 데이터 자동 관리 규칙

### 🔄 세션 시작 시 자동 체크 (필수)
매 세션에서 판사 관련 작업이 감지되면, 자동으로 다음을 수행:
1. **웹 검색**: "내란전담재판부 판사 명단 최신" 검색
2. **비교**: `src/data/judges.js`의 현재 판사 목록과 대조
3. **누락 감지**: 새로 배정/변경된 판사가 있으면 자동 추가
4. **사용자 알림**: "판사 X명 누락 발견, 자동 추가합니다" 안내

### 새 판사 추가 시 자동 완성 항목 (사진 외 전부 자동)
| 항목 | 자동화 | 방법 |
|------|--------|------|
| 기본 정보 (이름, 소속, 직위) | ✅ 자동 | 웹 검색 |
| career (경력) | ✅ 자동 | 웹 검색 (나무위키, 법률신문 등) |
| cases (주요 판결) | ✅ 자동 | 웹 검색 (뉴스 기사) |
| justiceEvaluation (사법정의평가) | ✅ 자동 | 판결 데이터 기반 AI 분석 |
| photo (인물 사진) | ❌ 수동 | 사용자가 public/에 직접 추가 |

### 판사 추가 자동 워크플로우
```
1. 웹 검색으로 판사 정보 수집 (경력, 판결, 뉴스)
2. justiceEvaluation AI 분석 생성 (검찰/재판부 이슈 포함)
3. judges.js에 완성된 데이터 삽입
4. npm run build로 검증
5. 사용자에게 "사진만 public/{이름}.png에 추가해주세요" 안내
6. 사진 추가 확인 후 배포
```

### judges.js 데이터 형식 (반드시 준수)
판사 데이터의 `cases` 필드는 다음 형식만 허용:
```javascript
cases: [
    { text: '사건 설명 (연도, 법원)', source: 'URL 또는 null' }
]
```
**금지 형식** (React Error #31 발생):
```javascript
// ❌ 절대 사용 금지
cases: [{ title: '...', date: '...', description: '...' }]
cases: [{ name: '...', detail: '...' }]
```

### 스크립트 (수동 사용 시)
```bash
node scripts/add-judge.cjs --name "판사이름" --category "내란전담재판부" --court "서울중앙지방법원" --position "형사합의00부 부장판사"
```

## 배포 전 체크리스트
- [ ] `npm run build` 오류 없음
- [ ] 로컬 프리뷰에서 변경사항 확인 완료
- [ ] SentencingAnalysis.jsx 미수정 확인
- [ ] AI 모델 기본값 `claude` 유지 확인
- [ ] 메인 페이지(`/`) 정상 로딩
- [ ] 블로그 목록/상세 페이지 정상
- [ ] SNS 공유 버튼 동작 확인 (카카오톡 이미지 정확성, X/Twitter, 텔레그램)
- [ ] 블로그 수정 시: Firestore 필드명 일치 확인 (`imageUrl`, NOT `featuredImage`/`thumbnailUrl`)
- [ ] X 공유 URL: `twitter.com/intent/tweet` 사용 확인 (intent/post 금지)
- [ ] 모바일 반응형 확인
- [ ] 관리자 페이지 접근 가능
- [ ] **사용자 배포 승인 받음**
- [ ] Git: 커밋할 모든 수정 파일이 staging 되었는지 `git status` 확인
- [ ] judges.js 수정 시: cases 필드 `{ text, source }` 형식 확인
- [ ] 판사 추가 시: 라이브 사이트 judge-evaluation 페이지에서 해당 카테고리 확인

## 복잡한 작업 프로세스 (팁 #2)
큰 기능 추가나 리팩토링 시 다음 순서를 따른다:
1. **계획 단계**: "plan this"로 요구사항 정리 → 작업 범위 확정
2. **구현 단계**: 계획에 따라 코드 작성 (서브에이전트에 위임)
3. **검증 단계**: architect 에이전트로 코드 리뷰 및 검증
4. **실패 시**: 즉시 재계획 → 다른 접근법 시도 (같은 방법 반복 금지)

적용 대상: 3개 이상 파일 수정, 새 기능 추가, 구조 변경 등

## 버그 수정 워크플로우 (팁 #5)
Firebase Functions 관련 버그 발생 시:
1. `firebase functions:log --only <함수명>` 으로 로그 수집
2. 로그 내용을 Claude에 제공하여 원인 분석 요청
3. 수정 후 반드시 로그 재확인으로 해결 검증
4. 프론트엔드 버그: 브라우저 콘솔 에러 메시지 복사 → Claude에 제공

## 변경 이유 설명 (팁 #10)
다음 영역 수정 시 반드시 "왜 이렇게 변경했는지" 설명을 포함할 것:
- `functions/index.js` — SSR, 크롤러 감지, 텔레그램 봇 로직
- `src/lib/auth.js` — 카카오 로그인 플로우 (모바일/데스크톱 분기)
- `src/main.jsx` — 라우팅 구조, 코드 분할
- SNS 공유 관련 코드 — 도메인 인코딩, 캐시 무효화 로직
