# Environment variables (API keys)
.env
.env.local
.env.production
backend/.env
# 중첩 경로의 모든 .env 차단 (src/.env 등) — 접근 코드/시크릿 노출 방지
**/.env
**/.env.*

# Firebase Service Account Key (절대 공유하면 안됨!)
serviceAccountKey.json
**/serviceAccountKey.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
"*.pdf" 
*.pdf 
trigger rebuild

.vercel

.vercel

# Functions environment variables
functions/.env
functions/.runtimeconfig.json
.vercel/.env*
src_backup_*/


# Claude 로컬 설정 (민감 정보 가능)
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/

# 임시 파일
temp_pdf/
nul

# Lighthouse 리포트 (큰 파일, 일회성)
lighthouse-*.json

# 빌드 산출물/배포 캐시 (추적 안 함 — 빌드 시 재생성)
.firebase/
public/og-trial-schedule.png
