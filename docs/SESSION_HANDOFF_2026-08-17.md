# 세션 인수인계 — 2026-08-17 (국과수 기본운영규정 확보 + 쟁점 출처 채우기 + 배포)

> 브랜치: `claude/appeal-column-5hwjiy` · 작업 환경: **로컬 세션** (`functions/.env` 확인)
> 앞 세션: `SESSION_HANDOFF_2026-08-08.md`
> **다음 세션은 다른 기기(데스크탑)에서 이어집니다.** 아래 Ⅱ를 먼저 읽으세요.

---

## Ⅰ. 이번 세션에서 한 것

| 커밋 | 내용 |
|---|---|
| `721337d` | 국과수 기본운영규정 확보 → `nfs-consolidation` 쟁점 완성 + 여덟 쟁점 전부에 우려 출처 부착 + 화면 렌더 결함 둘 수정 |

**push 완료**(`864073c..721337d`), **호스팅 배포 완료**(`firebase deploy --only hosting`,
45개 파일). 라이브 확인: `/law-diff` 카드 8개, 「⚠ 출처 미확인」 0건, 콘솔 에러 없음,
메인·블로그 200.

### 1. 국과수의 부·과는 대통령령에 없었다

행안부 직제 제54조제1항이 하부조직과 분장사무를 **기본운영규정(예규 제577호)**에
위임한다. 그 예규를 받아 `docs/bills/국립과학수사연구원_기본운영규정.txt` 로 넣고,
대검 과학수사부(검찰청 직제 제9조의2) 네 개 과와 항목별로 대비했다.
대비 결과 전문은 `docs/bills/README.md` 「국립과학수사연구원 기본운영규정 — 2026-08-17 확인」 절.

가장 큰 소득 셋:

- **감정 3개 과는 국과수에 대응 조직이 있고, 사이버ㆍ기술범죄수사과는 대응이 없다.**
  그 과의 사무는 「검찰사무의 지휘ㆍ감독 및 수사지원」이라 감정이 아니라 수사다.
  「과수부를 통째로 옮긴다」는 말에 이 과가 묻어가면 수사 조직 하나가 논의 없이 이동한다.
- **「중립 제3기관」 전제가 조문상 무너진다.** 행안부장관 소속인 데 더해 **본원
  행정지원과장이 총경**(제13조제1항), **지방 4개소 행정운영과장이 경정**(제17조제2항 등),
  유전자과는 **경찰청장 위탁사무**와 **디엔에이 DB 운영**을 맡는다(제14조제4항).
  이 사실들은 대통령령·부령만 읽으면 보이지 않는다.
- **앞 세션의 서술을 하나 바로잡았다.** 「국과수로 가면 공개 수준이 낮아진다」는 틀렸다 —
  기본운영규정도 국가법령정보센터에 공개된다. 갈리는 것은 **공개 여부가 아니라 고치는
  절차의 무게**다(국무회의 심의·법제처 심사 없음, 그 아래로 제19조·제25조제1항이 원장 소관).

### 2. 여덟 쟁점 전부에 우려 출처를 붙였다

「⚠ 출처 미확인」 배지가 0건이 됐다. 새로 확보한 1차 자료는
**참여연대 사법감시센터 공개질의서**(2026-07-27, `peoplepower21.org/judiciary/2026704`).

> **주의 — 참여연대의 것은 주장이 아니라 질의다**(「…계획이 있습니까?」).
> 「단체가 통합을 주장했다」로 옮겨 적으면 2026-03-18 사건과 같은 종류의 오류가 된다.
> `concern.claim` 본문에 이 구분을 적어 두었다.

`search-seizure-recording` 이 가장 부실했다(실질 출처 하나). 발의자 취지(아시아경제
2026-07-23 문혜원)와 법관 해설(법률저널 2026-08-13 손호영 판사)을 더했고, 후자가 지적한
**무관 개인정보의 보관·폐기** 항목이 우리 안에 빠져 있어 ④로 넣었다.

바로잡은 것: **MBC 「경찰에 강력 경고」의 발언 주체는 이재명 대통령**이었고,
**정성호 장관 발언의 보도 출처는 KBS**(김영훈, 2026-08-05)였다.

### 3. 화면 결함 둘 — 앞선 데이터부터 영향받고 있었다

- `otherProposals` 가 **렌더되지 않고 있었다.** 스키마에는 있는데 카드에 블록이 없었다.
  「우리 안」(보라 2px)과 구분되게 회색 1px + 「다른 곳에서 나온 안 — 우리 제안이 아닙니다」로 추가.
- `ourProposal.rationale` 과 `issue.note` 가 `<p>` 생출력이라 `**강조**` 가 **별표째
  화면에 나오고 있었다.** `RichText` 로 감쌌다.

---

## Ⅱ. ⚠ 다른 기기로 옮길 때 — 이 기기에만 있는 것

`git push` 는 마쳤으므로 **커밋된 것은 데스크탑에서 `git pull` 하면 전부 온다.**
문제는 git 에 올라가지 않는 것들이다.

### 1. 국과수 기본운영규정 PDF 원본 — 넘어가지 않는다

```
docs/bills/국립과학수사연구원_기본운영규정(제577호)(20260101).pdf
```

`.gitignore` 의 `*.pdf` 에 걸려 있다. **추출 텍스트(.txt)는 커밋됐으므로 조문 대조는
데스크탑에서도 된다.** PDF 가 필요한 경우는 하나뿐이다 — **별표 2(정원)**. 이 PDF 에는
별표가 붙어 있지 않으므로, 정원을 확인하려면 어차피 국가법령정보센터에서 별표 포함본을
다시 받아야 한다. 즉 **이 PDF 를 굳이 옮길 이유는 없다.**

### 2. 커밋하지 않은 파일들 — 데스크탑에는 없다

이번 작업과 무관하지만 이 기기에만 있는 것들이다. 필요하면 옮기거나 커밋할 것.

| 파일 | 성격 |
|---|---|
| `public/20260719다시,제헌절.png`, `public/명태균.png` | **이미지 — 쓸 계획이 있으면 커밋해야 사이트에 나간다** |
| `docs/stats/enbanc_형사_2026-08-07.json` | 수집 결과 |
| `functions/*.cjs` 8건 (블로그·서명 스크립트) | 일회성 작업 스크립트 |
| `backups/firestore_backup_2026-07-11T04-51-22.json` | 백업 |
| `check_blog_out.txt`, `collect_out.txt` | 실행 로그(버려도 무방) |

### 3. 환경 판별에 관한 보정 — CLAUDE.md 가 덜 정확하다

CLAUDE.md 는 「`ls functions/.env` → 있으면 데스크탑」이라고 적고 있지만, 정확히는
**「있으면 로컬 세션」**이다. **어느 기기인지는 알려 주지 않는다.** 이번 세션이 그 예다 —
`functions/.env` 가 있었으나 데스크탑이 아니었다.

실무상 차이는 작다(로컬이면 함수 배포·수집·프리뷰가 모두 된다). 다만 **「데스크탑에
넣어 둔 파일이 보인다」는 추론은 성립하지 않는다** — 그건 같은 기기일 때만이다.
문구를 고칠지는 사용자 판단에 맡긴다.

---

## Ⅲ. 데스크탑에서 이어서 할 것

우선순위 순.

### 1. 남은 미완 셋 (모두 데이터에 사유가 적혀 있다)

| 무엇 | 상태 | 어떻게 |
|---|---|---|
| **국과수 정원** | 별표 2 미확보 | 국가법령정보센터에서 **별표 포함본**을 받는다. 경찰공무원 수와 감정 인력 규모를 대검 과수부와 비교할 수 있게 된다 |
| **봉지욱(매봉쇼) 회차 URL** | 5개 쟁점에서 빈칸 | 「대검 과학수사부… 거기가 바로 '검찰청 캐비닛'」 쇼츠(`youtube.com/shorts/-__ZIDcOt6g`)가 후보였으나 **채널·게시일을 확인하지 못해 넣지 않았다.** 사용자가 회차를 아시면 그것으로 채운다 |
| **`news.kbs.co.kr` · `www.fnnews.com`** | 대조 실패 | 각각 접속 불가·403. 브라우저로 직접 열어 제목·일자·본문을 맞추고 `source` 의 실패 문구를 지운다 |

> 셋 다 **지우지 말 것.** 「확인 못 했다」가 독자가 알아야 할 정보다(설계문서 Ⅱ-5).
> `src/data/lawIssues.js` 머리 주석에 같은 취지를 적어 두었다.

### 2. `/law-diff` 의 SSR 메타 — 점검 안 했다

이 페이지 링크를 SNS 에 붙여넣을 계획이면 먼저 볼 것. `functions/index.js` 에
`/law-diff` 용 메타(제목·OG 이미지)가 있는지 확인하고, 없으면 추가 후
**`firebase deploy --only functions:<함수명>`** 이 따로 필요하다. 호스팅 배포로는 반영되지 않는다.
(공유 버튼은 화면 DOM 의 `og:image` 라 이미 배포된 것으로 동작한다 — **경로가 둘**이다.)

### 3. 설계문서의 진행 상황 표가 낡았다

`docs/analysis/수사기소분리_쟁점분석_설계.md` 「진행 상황」은 3~7단계를 「미착수」로
적고 있는데, 실제로는 3(조문 대비)·5(ISSUES)·6(우리 안)·7(화면)이 상당히 진행됐고
4(보도 수집)는 이번에 끝났다. **부분적으로 고치면 다른 쟁점까지 끝난 것처럼 읽혀서
손대지 않았다.** 한 번에 정리할 것.

### 4. 앞 세션에서 넘어온 미해결

`SESSION_HANDOFF_2026-08-08.md` Ⅰ-1 의 것 — **CI 텔레그램 알림이 401 Unauthorized**.
`TELEGRAM_BOT_TOKEN` 시크릿 재발급이 필요하다. 아직 안 됐다.

---

## Ⅳ. 상태 요약

```
브랜치   claude/appeal-column-5hwjiy
원격     721337d 까지 push 완료 (origin 과 동일)
배포     호스팅 배포 완료 · 함수 배포는 불필요(functions/index.js 미수정)
빌드     npm run build 통과
쟁점     8건 전부 status: 'draft' — 공개 단계는 페이지 전체가 함께 넘어간다
```

데스크탑에서 시작할 때:

```bash
git pull origin claude/appeal-column-5hwjiy
```
