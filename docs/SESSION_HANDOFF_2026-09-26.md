# 세션 인수인계 2026-09-26 — 칼럼 「선명성이 문제인가, 대통령의 의지가 문제인가」 게시·블로그 도구 정리

앞선 인수인계: `docs/SESSION_HANDOFF_2026-09-24.md`(대법관 칼럼 게시·사법뉴스 법률신문·9.22 선고). 이 문서는 9.25~9.26에 한 일과 **왜 그렇게 했는지**만 적는다. 목록은 `git log 32f13d4..HEAD`.

## 0. 환경·상태
- 데스크탑(로컬) 세션, main, 28dfc83 까지 push(이 문서 커밋 포함 시 그 다음).
- 호스팅·함수 배포 없음(이번 작업은 전부 Firestore 글 데이터와 Storage 이미지). 9.25 게시 직전 Firestore 백업 `backups/firestore_backup_2026-09-25T14-35-32.json`(281개).
- 미추적(의도적): `public/20260922위임을 벗어난 대리인.hwpx`, 카드뉴스 mp4 1개, `scripts/audio/news-three/_*.mp3` 테이크 예비본.

## 1. 새로 게시한 칼럼
- **「선명성이 문제인가, 대통령의 의지가 문제인가」** — post `jRdIwotc7JGxFdF4vKjk`, 부제(subtitle 필드) 「대통령의 국가보안법 비유에 부쳐」. 원고 `docs/columns/2026-09-25_선명성이_문제인가_조문이_문제인가.md`(파일명은 첫 제목 그대로).
- 계기: 9.24 멕시코 동포 간담회에서 대통령이 2004년 국보법 개정 무산을 들어 「선명해서 멋있을지는 몰라도…」(유시민 작가 겨냥으로 보도).
- **사실 바로잡기(중요)**: 처음에 「2004년 장벽은 야당 반대」라고 썼다가 사용자 지적으로 정정 — 12.31 4자회담 합의안(5대 독소조항 삭제)을 깬 것은 **여당 의총**(프레시안 기고·한국일보 당시 보도). 그래서 논지는 「대통령 서술은 맞다, 그러나 2026년엔 막힌 법안도 타협 상대도 없다」로 세웠다.
- **「의총조차 없다」는 쓰지 않았다**: 민주당은 7.21·7.24 정책의총에서 보완수사권 폐지 당론 추인. 그래서 「당론을 9월 법률·직제가 비켜 갔는데 다시 묻지 않았다」로 썼다(사실에 맞고 더 아프다).
- 「하수인」 대신 헌법 제1조·제40조·제46조② 부합 여부를 묻는 방식(사용자 제안).
- 구조: 대법관 칼럼과 틀이 같아 「AI가 쓴 티」가 난다는 지적 → 번호 절·요약 머리글·표·열거 소제목 제거, 2004.12.31 의총 장면에서 시작하는 흐름. 맺음은 「물어야 할 것은 선명성이 아니라 대통령의 의지다」.
- 추가 논지: 「불가역」 두 길(대통령령=법무부 직제 / 법률=검사정원법·정부조직법) 모두 멈춤, 9.10 대통령 「정원법에 얽매일 필요 없이」(서울신문 전언) = 가역의 길 선택, 황운하 검사정원법안(9.8, 2,292→1,528) vs 민주당 무발의. 국보법 폐지법률안(2214785, 민형배 등 31인, 법사위 심사 중)만 있고 독소조항 삭제 정부안·당론안 없음 → 「교훈은 들려주는 사람이 먼저 따를 때 교훈」.
- **게시 전 확정 못 한 사실**(원고 말미 「확인할 것」): 9.18·9.24 발언 청와대 전문 대조, 9월 민주당 의총 안건, 법무부 직제 연혁(1년간 탈검찰화 개정 없음), 민주당 검사정원법안 유무, 국보법 폐지안 심사 단계. 틀린 것이 나오면 본문 수정.

## 2. 사용자 편집과 동기화 — 이번에 정착한 방식
- 사용자가 관리자 화면(Quill)에서 오탈자·문안을 직접 고친다. **Quill 저장은 인라인 스타일·hr·class 를 전부 벗긴다**(p/strong/ul/li/br/img 만 남음).
- 절차: ① `functions/dump_blog_post.cjs`(읽기 전용)로 라이브 본문을 내려받아 백업 ② md 변환본과 문단 단위 diff ③ 사용자 문안을 **md 에 먼저 옮김** ④ 필요할 때만 `update_blog_body.cjs`로 서식 입혀 재게시(교체 전 diff 가 0~오타 1곳인지 확인).
- 문장 한두 개 수정은 **`replace_in_blog_post.cjs`**(정확히 1회 일치해야 진행, 백업)로 해서 사용자 편집을 보존한다. 9.26 사용자 편집 9곳은 md 에 반영, 「「추진」한다.였다.」의 「였다.」는 오타로 보고 재게시 때 빠짐(사용자에게 알림).
- 서식: 자료 목록 0.85em·항목 간격 10px, 마디는 hr + **빈 문단**(Quill 저장 뒤에도 빈 줄로 구분이 남게).

## 3. 표지
- 1차: 애니메이션 GIF(2004 시끄러운 의총 vs 2026 「특이사항 없음」) — `scripts/blog-covers/sharpness-vs-provisions-20260925.html`. 제목 변경 때 v3 새 파일명으로 교체.
- **현재: 삽화판 JPG** — 사용자가 Gemini 로 만든 수채 삽화(`scripts/blog-covers/sharpness-illustration-20260926.webp`, 오른쪽 아래 생성기 표식은 지우지 않음) 위에 제목·연도 표시를 `sharpness-illustrated-20260926.html` → `render_still.mjs` 로 얹음. 교체는 `swap_blog_cover.cjs`(새 파일명 강제, 본문은 이미지 주소만 치환).
- 방식 정리: **그림은 사용자가 이미지 생성기로(글자 없이, 아래 1/4 비움), 한글 글자는 우리가 얹는다.** 이 세션엔 이미지 생성 도구가 없다. 실존 인물은 닮게 그리지 않고 직함·소품·뒷모습으로. 타 매체 만평(예: 한겨레 「논썰」 권범철 그림)은 쓰지도 화풍 흉내 요청도 하지 않는다.
- **대기 중**: 같은 글의 풍자 캐리커처판 표지 — 사용자에게 생성 문구를 줬다(연단에서 확성기 든 사람을 손가락질하는 대통령 / 먼지 쌓인 법전 세 권·안 연 도장 / 입에 지퍼 채운 의원들). 그림이 오면 글자(제목·부제·「법무부 직제」「검사정원법 2,292명」「국가보안법 독소조항」「특이사항 없음」「선명성?」)를 얹어 먼저 보여 주고 승인 뒤 교체.

## 4. 발견한 버그 — 별도 작업으로 띄움(task_3dbf0cfa)
- 라이브 블로그 글 페이지에서 react-helmet-async 메타(data-rh)가 하나도 없고 document.title·og:image 가 index.html 기본값(og-world-news.png)이다. 대법관 글도 같음. 몇 분 전엔 정상이었던 적이 있어 간헐적일 수 있다.
- 영향: 링크 붙여넣기 미리보기(SSR)는 정상. **카카오 공유 버튼**(SNSShareBar.getOgImage 가 DOM og:image 를 읽음)은 기본 이미지를 보낼 수 있다. 수정·배포는 그 작업에서 승인받고.

## 5. 보관 메모
- `docs/columns/2026-09-25_메모_대법관_제청권_헌법사_1962-1972-1987.md` — 1962 헌법 제99조②(법관추천회의 동의 얻은 제청 → 「임명하여야 한다」), 1972 유신 삭제, 1980·1987 미복원. 논지 「빈칸이 아니라 지워진 자리」, 「구속력은 합의체에 붙어 있었다」. 개헌·법원조직법 개정 논의 때 쓴다(사용자 결정: 지금은 킵).

## 6. 도구 목록(이번에 생김)
| 파일 | 용도 |
|---|---|
| `scripts/blog-covers/md2blog.py` | 범용 md→블로그 HTML(`> **` 배너 제외, `> ` 인용상자, 표, 자료 소형, hr+빈 문단) |
| `scripts/blog-covers/render_cover_gif.mjs` | 애니메이션 표지 HTML → GIF/MP4/포스터(`window.seek`) |
| `scripts/blog-covers/render_still.mjs` | 정지 표지 HTML → PNG+JPG |
| `functions/create_blog_post_sharpness.cjs` | 이 글 addDoc(백업 후) — 다음 글은 이것을 본떠 |
| `functions/replace_in_blog_post.cjs` | 문장 단위 치환(백업, 1회 일치) |
| `functions/update_blog_body.cjs` | 본문 전체 교체(백업) — 사용자 편집을 md 에 옮긴 뒤에만 |
| `functions/swap_blog_cover.cjs` | 표지 교체(새 파일명 강제, 주소만 치환) |
| `functions/dump_blog_post.cjs` | 라이브 본문 내려받기(읽기 전용) |

## 7. 다음에 할 일
1. 캐리커처판 표지 그림이 오면 글자 얹어 보여 주기 → 승인 뒤 `swap_blog_cover.cjs`.
2. 칼럼 「확인할 것」 5건 확정(특히 9월 민주당 의총 안건, 청와대 전문).
3. task_3dbf0cfa(Helmet 메타 누락) 진행 여부 사용자 확인.
4. 9.24 인수인계의 미결: 사법뉴스 06:00 수집 결과(법률신문 섹션) 확인, 공소청 직제 관보 대조, 정성호 「3개월」 출처, 현빈 녹음 교체, 「반도체 세수」 영상 배경음악.

## 8. 추가 (9.26 오후)
- **블로그 목록 카드 이미지**: object-contain+흐린 배경으로 바꿔 배포했다가 사용자 요청으로 **object-cover(칸에 꽉 참)로 되돌려 배포**(b3191a7). 코드에 「다시 바꾸지 말 것」 메모. 카드 제목 `break-keep`(낱말 단위 줄바꿈)은 유지.
- 그래서 **표지는 제목을 위쪽 75% 안에 둔다**(카드가 16:9 로 위쪽만 보여 줌). 「선명성이 문제인가…」 표지를 제목 위쪽 띠 + 삽화 아래 배치로 다시 만들어 교체(`scripts/blog-covers/sharpness-illustrated-top-20260926.html`, Storage `sharpness-vs-will-illustrated-top-20260926.jpg`). 원 그림 아래 빈 띠(생성기 표식)가 잘려 나가 「그림: AI 생성」을 표기.
- **텔레그램 알림 401**: 9.24 14:36 `.env` 봇 토큰 교체 뒤 개혁안 뉴스 함수만 재배포돼 나머지가 옛 토큰으로 실패. 사용자 승인 후 `firebase deploy --only functions`(전체) 재배포, 누락 3건(9.24·9.25 사법뉴스, 9.25 칼럼 새 글) 「지연 알림」 표시로 재전송(메시지 1286~1288, `functions/resend_missed_alerts_20260926.cjs`). CLAUDE.md 에 규칙 추가. **내일 06:00 사법뉴스 알림이 오는지 확인해야 해결 확정.**
- **별도 세션 진행 중**: task_3dbf0cfa(블로그 글 페이지 Helmet 메타 누락 → og:image 기본값). 사용자가 다른 로컬 세션에서 시작함. 그 세션이 `BlogPost.jsx`/`SEOHead.jsx`/`main.jsx` 를 고칠 수 있으니 여기서는 그 파일들을 건드리지 말 것.

### 다음에 할 일(갱신)
1. 9.27 06:00 사법뉴스 텔레그램 알림 수신 확인(안 오면 `firebase functions:log` 에서 Telegram response 확인).
2. task_3dbf0cfa 결과 확인(끝나면 이 세션에 알림이 옴).
3. 캐리커처판 표지 그림 대기(생성 문구 전달함) — 오면 제목을 위쪽에 얹는 배치로.
4. 칼럼 「확인할 것」 5건, 9.24 인수인계 미결 항목.
